import 'package:flutter/material.dart';

class nlpapis extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("NLP APIS PAGE")),
    );
  }
}
